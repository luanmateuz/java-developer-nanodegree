spring:
  application:
    name: sdj
  datasource:
    url: jdbc:mysql://localhost:3306/jpa_mysql?useSSL=false&createDatabaseIfNotExist=true&&allowPublicKeyRetrieval=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update

logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
